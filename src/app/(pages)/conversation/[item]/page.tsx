import { ParamsTypes } from '@/utils/types'
import React from 'react'

const Conversation = ({ params }: ParamsTypes) => {
  console.log("params")
  return (
    <div>Conversation</div>
  )
}

export default Conversation